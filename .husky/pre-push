#!/usr/bin/env sh

# Pre-push hook: Rodar testes antes do push

echo ""
echo "🧪 Rodando testes antes do push..."
pnpm test:run

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Testes falharam! Push bloqueado."
  echo "💡 Corrija os erros e tente novamente."
  exit 1
fi

echo ""
echo "✅ Todos os testes passaram!"